export const publicRoutes = ["/", "/api/trial", "/api/task"];

export const privateRoutes = ["/api", "/todos"];

export const authRoutes = ["/login", "/signup"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
